// Domain types for comments.
//
// `Comment` stays in the repo-root `types.ts` for now — shared vocabulary —
// and is re-exported so callers take everything about the domain from
// `features/comments`.

export type { Comment } from '../../types';
